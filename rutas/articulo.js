const express =  require("express");
const router = express.Router();

// carga de controladores
const ArticuloController = require("../controladores/articulo");

// Rutas prueba
router.get("/ruta-de-prueba", )